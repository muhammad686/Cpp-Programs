#include<iostream>
#include <cmath>
#include <conio.h>
#include <iomanip>
#include<stdio.h>
using namespace std;

double powfun(double x, double y)
{
	double pow;
 	double result;
	result =pow(x,y);
	cout<<result;
}
int main()
{
	int a,b,c;
	cout<<"enter the value of a: ";
	cin>>a;
	cout<<"enter the value of b: ";
	cin>>b;
	powfun(a,b);
	pow(a,b;)
	return 0;
}
